<%= form_for [description.product, description] do |f| %>
	<div class="field">
		<%= f.label :description , 'any description?' %><br>
		<%= f.text_field :description %>
	</div>
	<div class="field">
		<%= f.submit %>
	</div>
<% end %>
