<%= form_for [comment.product, comment] do |f| %>
	<div class="field">
		<%= f.label :comment , 'any comment?' %><br>
		<%= f.text_field :comment %>
	</div>
	<div class="field">
		<%= f.submit %>
	</div>
<% end %>
